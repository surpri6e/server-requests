/**
    @example
    interface IUserTodos {
        completed: boolean;
        id: number;
        title: string;
        userId: number;
    }
 */
export interface IUserTodos {
    completed: boolean;
    id: number;
    title: string;
    userId: number;
}
/**
    @example
    interface IUserPhotos {
        albumId: number;
        id: number;
        title: string;
        url: string;
        thumbnailUrl: string;
    }
 */
export interface IUserPhotos {
    albumId: number;
    id: number;
    title: string;
    url: string;
    thumbnailUrl: string;
}

